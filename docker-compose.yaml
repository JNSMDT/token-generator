services:
  token-generator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: token-generator
    env_file:
      - .env
    ports:
      - '${APP_PORT}:3000'
    restart: unless-stopped
